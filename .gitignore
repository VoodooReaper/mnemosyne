# Secrets / env
.env
.env.*
*.key
*.pem
secrets/

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Build / deps
target/
build/
node_modules/
dist/
*.class

# Scratch
tmp/
scratch/

# NOTE: company data must never be committed here in the first place
# (this repo is design-only). This list is hygiene, not a data firewall.
